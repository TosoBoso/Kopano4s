#!/bin/sh
# (c) 2017-19 vbettag -install for kopano4s in Debian-Chroot container
# script for custom post-build activities e.g. move and softling etc areas into /etc/kopano

#mv /etc/xyz /etc/kopano/xyz
#ln -sf /etc/kopano/xyz /etc/xzy

exit 0
